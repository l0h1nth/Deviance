export type Alert = {id:number;timestamp:string;user_id:string;device_id:string;predicted_attack:string;risk_score:number;severity:string;confidence:number;status:string;explanation:string};
export type Metrics = {total_events:number;total_alerts:number;critical_alerts:number;open_investigations:number;false_positive_rate:number;average_detection_latency_ms:number;attacks_by_type:Record<string,number>;risk_trend:{id:number;risk:number}[]};
export type AlertDetail = Alert & {event:Record<string,unknown>;features:Record<string,number>;anomaly_score:number;class_probabilities:Record<string,number>;baseline_type:string;baseline_confidence:number;model_version:string;feature_schema_version:string;explanation_detail:{text:string;recommended_actions:string[];top_contributing_features:{feature:string;value:number;deviation:number;description:string}[]};feedback:{status:string;analyst:string;comment:string;created_at:string}[];timeline:Record<string,unknown>[]};

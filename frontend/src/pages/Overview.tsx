import {Activity,AlertTriangle,Clock3,Radio,ShieldAlert,Target} from 'lucide-react';import {Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {StatCard} from '../components/StatCard';import {RiskPill} from '../components/RiskPill';import type {Alert,Metrics} from '../types';
export function Overview({metrics,alerts,onSelect}:{metrics:Metrics|null;alerts:Alert[];onSelect:(a:Alert)=>void}){if(!metrics)return <div className="loading">Calibrating telemetry…</div>;return <><header className="page-head"><div><span className="eyebrow">COMMAND CENTER</span><h1>Behavior overview</h1><p>Live behavioral risk across identities, devices, and resources.</p></div><div className="live"><i/> LIVE MODEL</div></header><section className="stats"><StatCard label="Events analyzed" value={metrics.total_events.toLocaleString()} detail="trusted + under review" icon={<Activity/>}/><StatCard label="Active alerts" value={metrics.total_alerts} detail={`${metrics.open_investigations} need attention`} icon={<ShieldAlert/>}/><StatCard label="Critical" value={metrics.critical_alerts} detail="immediate response" icon={<AlertTriangle/>}/><StatCard label="Detection latency" value={`${metrics.average_detection_latency_ms}ms`} detail="average inference" icon={<Clock3/>}/></section><section className="grid-main"><article className="panel chart"><div className="panel-title"><div><span>RISK SIGNAL</span><h2>Live risk trend</h2></div><Target/></div><ResponsiveContainer width="100%" height={270}><AreaChart data={metrics.risk_trend}><defs><linearGradient id="riskFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#ff5d73" stopOpacity={.35}/><stop offset="100%" stopColor="#ff5d73" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#203047" vertical={false}/><XAxis dataKey="id" hide/><YAxis domain={[0,100]} stroke="#61738c"/><Tooltip contentStyle={{background:'#0e1b2c',border:'1px solid #26384f'}}/><Area type="monotone" dataKey="risk" stroke="#ff5d73" fill="url(#riskFill)" strokeWidth={2}/></AreaChart></ResponsiveContainer></article><article className="panel attack-mix"><div className="panel-title"><div><span>MODEL VERDICT</span><h2>Detections by type</h2></div><Radio/></div><div className="bars">{Object.entries(metrics.attacks_by_type).map(([name,count])=><div key={name}><label><span>{name.replaceAll('_',' ')}</span><b>{count}</b></label><div><i style={{width:`${Math.min(100,count/Math.max(metrics.total_events,1)*600)}%`}}/></div></div>)}</div></article></section><section className="panel alerts"><div className="panel-title"><div><span>PRIORITY QUEUE</span><h2>Latest alerts</h2></div><button>View all →</button></div><AlertTable alerts={alerts.slice(0,6)} onSelect={onSelect}/></section></>}
export function AlertTable({alerts,onSelect}:{alerts:Alert[];onSelect:(a:Alert)=>void}){return <div className="table-wrap"><table><thead><tr><th>Time</th><th>Identity</th><th>Attack hypothesis</th><th>Risk</th><th>Confidence</th><th>Status</th></tr></thead><tbody>{alerts.map(a=><tr key={a.id} onClick={()=>onSelect(a)}><td>{new Date(a.timestamp).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</td><td><b>{a.user_id}</b><small>{a.device_id}</small></td><td>{a.predicted_attack.replaceAll('_',' ')}</td><td><RiskPill score={a.risk_score} severity={a.severity}/></td><td>{Math.round(a.confidence*100)}%</td><td><span className="status">{a.status.replaceAll('_',' ')}</span></td></tr>)}</tbody></table>{!alerts.length&&<div className="empty">No alerts yet. Run the live simulator to populate this queue.</div>}</div>}


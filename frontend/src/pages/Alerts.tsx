import {Search} from 'lucide-react';import {useState} from 'react';import {AlertTable} from './Overview';import type {Alert} from '../types';
export function Alerts({alerts,onSelect}:{alerts:Alert[];onSelect:(a:Alert)=>void}){const[q,setQ]=useState('');const shown=alerts.filter(a=>JSON.stringify(a).toLowerCase().includes(q.toLowerCase()));return <><header className="page-head"><div><span className="eyebrow">INVESTIGATION QUEUE</span><h1>Live alerts</h1><p>Model-ranked behavior requiring analyst attention.</p></div></header><section className="panel alerts full"><div className="search"><Search/><input placeholder="Search identity, device, attack…" value={q} onChange={e=>setQ(e.target.value)}/><span>{shown.length} alerts</span></div><AlertTable alerts={shown} onSelect={onSelect}/></section></>}


import {Activity,BrainCircuit,GitCompareArrows,LayoutDashboard,ShieldCheck,Users} from 'lucide-react';import {useCallback,useEffect,useState} from 'react';import {api} from './services/api';import type {Alert,AlertDetail,Metrics} from './types';import {Overview} from './pages/Overview';import {Alerts} from './pages/Alerts';import {Investigation} from './pages/Investigation';import {ModelPage} from './pages/Model';import {DriftPage} from './pages/Drift';import {UsersPage} from './pages/Users';import {useLive} from './hooks/useLive';
type Page='overview'|'alerts'|'model'|'drift'|'users'|'investigation';
export default function App(){const[page,setPage]=useState<Page>('overview'),[metrics,setMetrics]=useState<Metrics|null>(null),[alerts,setAlerts]=useState<Alert[]>([]),[detail,setDetail]=useState<AlertDetail|null>(null),[model,setModel]=useState<any>(null),[drift,setDrift]=useState<any[]>([]);const load=useCallback(()=>{api.metrics().then(setMetrics).catch(()=>{});api.alerts().then(setAlerts).catch(()=>{});api.drift().then(setDrift).catch(()=>{})},[]);useEffect(()=>{load();api.model().then(setModel).catch(()=>{})},[load]);useLive(load);const select=(a:Alert)=>{setPage('investigation');api.alert(a.id).then(setDetail)};const update=(status:string)=>detail&&api.updateAlert(detail.id,status).then(()=>api.alert(detail.id).then(setDetail).then(load));const nav=[['overview',LayoutDashboard,'Overview'],['alerts',ShieldCheck,'Live alerts'],['users',Users,'User behavior'],['model',BrainCircuit,'Model performance'],['drift',GitCompareArrows,'Concept drift']] as const;return <div className="shell"><aside className="sidebar"><div className="brand"><span><Activity/></span><div><b>DEVIANCE</b><small>BEHAVIOR INTELLIGENCE</small></div></div><nav>{nav.map(([id,Icon,label])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id as Page)}><Icon/>{label}{id==='alerts'&&alerts.length>0?<em>{alerts.length}</em>:null}</button>)}</nav><div className="model-state"><i/><div><b>Model online</b><small>{model?.model_version||'Waiting for artifact'}</small></div></div></aside><main>{page==='overview'&&<Overview metrics={metrics} alerts={alerts} onSelect={select}/>} {page==='alerts'&&<Alerts alerts={alerts} onSelect={select}/>} {page==='investigation'&&<Investigation detail={detail} onBack={()=>setPage('alerts')} onUpdate={update}/>} {page==='model'&&<ModelPage model={model}/>} {page==='drift'&&<DriftPage drift={drift}/>} {page==='users'&&<UsersPage initialUser={alerts[0]?.user_id}/>}</main></div>}
